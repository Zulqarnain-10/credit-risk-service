"""credit_risk: a credit-default risk model shipped as a product.

Pipeline modules (data, validate, features, split, train, evaluate, fairness, monitoring)
build the artifacts under models/ and reports/. The api package serves them.
"""

__version__ = "0.1.0"
