"""FastAPI service that serves the trained credit-default model and the demo page.

Entry points:
    python -m credit_risk.api      run uvicorn on settings.api_port()
    credit_risk.api.app:app        ASGI application for any server
"""
